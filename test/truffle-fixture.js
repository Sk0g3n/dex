const Dex = artifacts.require("Dex");
const SwappableToken = artifacts.require("SwappableToken");
const SwappableToken2 = SwappableToken;

module.exports = async (deployer, network, accounts) => {
    /*const swappabletoken1 = await SwappableToken.new("suka", "suk", 10000);
    SwappableToken.setAsDeployed(swappabletoken1);
    const swappabletoken2 = await SwappableToken2.new("blyat", "bly", 10000);
    SwappableToken2.setAsDeployed(swappabletoken2);
    const dex = await Dex.new(swappabletoken1.address, swappabletoken2.address);
    Dex.setAsDeployed(dex);
    */
   
}